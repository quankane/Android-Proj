<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".activity.ProfileActivity">

    <!-- Header -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/headerLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="16dp"
        android:layout_marginTop="48dp"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/titleTxt"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/profile_title"
            android:textColor="@color/black"
            android:textSize="25sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageView
            android:id="@+id/backBtn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@+id/titleTxt"
            app:layout_constraintBottom_toBottomOf="@+id/titleTxt"
            app:srcCompat="@drawable/back" />
    </androidx.constraintlayout.widget.ConstraintLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/headerLayout">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Profile Info Card -->
            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/blue_bg"
                android:padding="20dp"
                android:layout_marginBottom="24dp">

                <ImageView
                    android:id="@+id/avatarImg"
                    android:layout_width="70dp"
                    android:layout_height="70dp"
                    android:background="@drawable/white_bg_circle"
                    android:padding="8dp"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    app:srcCompat="@drawable/ic_user_profile"
                    android:tint="@color/blue"
                    />

                <TextView
                    android:id="@+id/usernameTxt"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="16dp"
                    android:text="@string/username_placeholder"
                    android:textColor="@color/white"
                    android:textSize="20sp"
                    android:textStyle="bold"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toEndOf="@+id/avatarImg"
                    app:layout_constraintTop_toTopOf="@+id/avatarImg" />

                <TextView
                    android:id="@+id/emailTxt"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:text="Email"
                    android:textColor="@color/white"
                    android:textSize="14sp"
                    app:layout_constraintStart_toStartOf="@+id/usernameTxt"
                    app:layout_constraintTop_toBottomOf="@+id/usernameTxt" />
            </androidx.constraintlayout.widget.ConstraintLayout>

            <!-- Menu Options (Simplified using LinearLayouts) -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/general_settings"
                android:textColor="@color/black"
                android:textSize="16sp"
                android:textStyle="bold"
                android:layout_marginBottom="8dp"/>

            <LinearLayout
                android:id="@+id/ordersLayout"
                style="@style/ProfileMenuItem"
                android:clickable="true"
                android:focusable="true">
                <ImageView style="@style/ProfileMenuIcon" app:srcCompat="@drawable/ic_order_history" />
                <TextView style="@style/ProfileMenuText" android:text="@string/order_history" />
                <ImageView style="@style/ProfileMenuArrow" app:srcCompat="@drawable/arrow_right" />
            </LinearLayout>

            <LinearLayout
                android:id="@+id/addressLayout"
                style="@style/ProfileMenuItem"
                android:clickable="true"
                android:focusable="true">
                <ImageView style="@style/ProfileMenuIcon" app:srcCompat="@drawable/ic_address" />
                <TextView style="@style/ProfileMenuText" android:text="@string/shipping_address" />
                <ImageView style="@style/ProfileMenuArrow" app:srcCompat="@drawable/arrow_right" />
            </LinearLayout>

            <LinearLayout
                android:id="@+id/settingsLayout"
                style="@style/ProfileMenuItem"
                android:clickable="true"
                android:focusable="true">
                <ImageView style="@style/ProfileMenuIcon" app:srcCompat="@drawable/ic_settings" />
                <TextView style="@style/ProfileMenuText" android:text="@string/settings" />
                <ImageView style="@style/ProfileMenuArrow" app:srcCompat="@drawable/arrow_right" />
            </LinearLayout>

            <!-- Logout Button -->
            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/logoutBtn"
                style="@android:style/Widget.Button"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="40dp"
                android:background="@drawable/red_bg"
                android:text="@string/logout"
                android:textColor="@color/white"
                android:textSize="18sp"
                android:textStyle="bold" />

        </LinearLayout>
    </ScrollView>
</androidx.constraintlayout.widget.ConstraintLayout>