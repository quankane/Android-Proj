<?xml version="1.0" encoding="utf-8"?>
<!--
    SỬA 1: Dùng CoordinatorLayout để Toolbar cuộn đẹp
    và fitsSystemWindows để xử lý thanh trạng thái.
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:fitsSystemWindows="true"
    tools:context=".activity.EditProfileActivity">

    <!-- SỬA 2: Thêm AppBarLayout để chứa Toolbar (cách làm đúng) -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fitsSystemWindows="true"
        app:liftOnScroll="true">

        <!--
            SỬA 3: Xóa các padding, textSize, textColor... không hợp lệ.
            Đổi Title cho đúng ngữ cảnh.
        -->
        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:navigationIcon="@drawable/back"
            app:title="Hồ sơ của tôi" />

    </com.google.android.material.appbar.AppBarLayout>

    <!--
        SỬA 4: Thêm app:layout_behavior để ScrollView
        luôn nằm dưới Toolbar (sửa lỗi chồng chéo).
    -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Avatar (Giữ nguyên, đã đẹp) -->
            <ImageView
                android:id="@+id/avatarImg"
                android:layout_width="100dp"
                android:layout_height="100dp"
                android:layout_gravity="center_horizontal"
                android:layout_marginBottom="24dp"
                android:background="@drawable/white_bg_circle"
                android:padding="8dp"
                app:srcCompat="@drawable/ic_user_profile" />

            <!--
                SỬA 5: Nâng cấp EditText thành TextInputLayout
                (hiện đại, có nhãn nổi, nhất quán với các form khác).
            -->
            <com.google.android.material.textfield.TextInputLayout
                style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="16dp"
                android:hint="Username">

                <com.google.android.material.textfield.TextInputEditText
                    android:id="@+id/usernameEdt"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:inputType="textPersonName" />
            </com.google.android.material.textfield.TextInputLayout>

            <!-- SỬA 6: Nâng cấp Email EditText -->
            <com.google.android.material.textfield.TextInputLayout
                style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="16dp"
                android:hint="Email">

                <com.google.android.material.textfield.TextInputEditText
                    android:id="@+id/emailEdt"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:enabled="false"
                    android:textColor="@color/darkGrey" />
            </com.google.android.material.textfield.TextInputLayout>

            <!-- Nút đổi mật khẩu (Giữ nguyên) -->
            <TextView
                android:id="@+id/changePasswordBtn"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end"
                android:layout_marginBottom="20dp"
                android:text="Change password"
                android:textColor="@color/blue"
                android:textSize="16sp"
                android:textStyle="bold" />

            <!--
                SỬA 7: Nâng cấp AppCompatButton thành MaterialButton
                và tạo khoảng cách (Space).
            -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <!-- Nút Logout (Outlined style cho ít nổi bật hơn) -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/logoutBtn"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Logout"
                    android:textColor="@color/red"
                    android:textSize="16sp"
                    app:strokeColor="@color/red" />

                <Space
                    android:layout_width="16dp"
                    android:layout_height="wrap_content" />

                <!-- Nút Save (Nút chính, style mặc định) -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/saveBtn"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Save Changes"
                    android:textSize="16sp" />
            </LinearLayout>

        </LinearLayout>
    </ScrollView>
</androidx.coordinatorlayout.widget.CoordinatorLayout>